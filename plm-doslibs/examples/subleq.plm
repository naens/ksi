subleq:
do;
$include(..\plmlib.inc)

    declare str (80) byte;
    call print_string(@('Input String is: ', 0));
    if read_line(length(str), @str) > 0 then
        call print_line(@str);
    call terminate(0);
end;
