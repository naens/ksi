;; Program: hello
;;
;;     Prints the 'Hello, world!' message.
;;

extrn	cons_prstr:near

	cseg

start:
	mov	ax, offset msg
	call	cons_prstr

	mov	cl, p_termcpm
	mov	dx, 0
	call	ccpm

ccpm:
	int	ccpmint
	ret

include bdos.def

	dseg

msg	db	'Hello, world!', 0

	end

